server {
	listen 80 default_server;
	listen [::]:80 default_server;
	server_name _;

	# Captive / wall garden: cualquier Host HTTP (dst-nat desde MikroTik) → aviso de pago
	location / {
		return 302 https://app.fibranexus.cl/suspended;
	}
}
