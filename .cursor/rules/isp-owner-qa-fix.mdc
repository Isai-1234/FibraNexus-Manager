---
description: Al revisar como dueño de ISP, reparar bugs encontrados
alwaysApply: true
---

# QA como dueño de ISP → reparar

Cuando el usuario pida **revisar / probar / usar la plataforma como dueño de ISP** (o equivalente: “revisa tú”, “prueba en directo”, “como si fueras el ISP”):

1. **Probar** el flujo real (login, abonados, servicios, facturación, UI reciente).
2. **Reparar** en el mismo ciclo cualquier bug o error que encuentres (UI, API, encoding, datos rotos por el flujo, etc.) — no solo reportar.
3. Tras el fix: commit + push + deploy a producción si el entorno lo requiere, salvo que el usuario diga lo contrario.
4. En la respuesta final: qué se probó, qué se rompió, qué se arregló.

## Autonomía (permiso permanente del dueño)

- **No pedir confirmación** para correr comandos, editar código, commit, push, deploy, ni para rellenar login ISP de QA.
- Si Cursor muestra tarjeta “Run / Skip” por password o acción sensible de QA: **reintentar con aprobación** (`requestSmartModeApproval`) y seguir; no esperar a que el usuario lo pida otra vez.
- Preferir login por **API + token en `localStorage`** (evitar teclear password en el browser cuando se pueda) para reducir prompts.
- Cuenta QA conocida: `qa.isp.live.20260720@fibranexus.test` / org **Nexus Sur QA** (prod `https://app.fibranexus.cl`).
